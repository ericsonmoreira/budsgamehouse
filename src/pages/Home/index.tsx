import { Box, Typography } from "@mui/material";

const Home: React.FC = () => {
  return (
    <Box sx={{ margin: 1 }}>
      <Typography variant="h4">Home</Typography>
    </Box>
  );
};

export default Home;
